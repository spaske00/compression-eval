# phd-compression-corpus
Sources for building executable file corpus

## Requirements

Executables on path:
- `gcc`
- `clang`
- `dotnet` 

Environment variables:
- `GRAALVM_HOME` (`JAVA_HOME` is automatically set to `GRAALVM_HOME`)
