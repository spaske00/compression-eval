# mimalloc corpus build

Pinned at `fef6b0dd70f9d7fa0750b0d0b9fbb471203b94cd`. Upstream documents CMake; the selected upstream executable is `mimalloc-test-api`, linked with the static allocator. Use `make CC=gcc-14 CXX=g++-14 TARGET_NAME=mimalloc-gcc-linux-x64-O2 CFLAGS=-O2`.
