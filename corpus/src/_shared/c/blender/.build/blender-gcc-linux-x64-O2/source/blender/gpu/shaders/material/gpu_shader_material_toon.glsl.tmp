#line 1 "gpu_shader_material_toon.glsl"
 
#line 8
void node_bsdf_toon(
    float4 color, float size, float tsmooth, float3 N, float weight, _ref(Closure ,result))
{
  color = max(color, float4(0.0f));
  N = safe_normalize(N);
#line 15
  ClosureDiffuse diffuse_data;
  diffuse_data.weight = weight;
  diffuse_data.color = color.rgb;
  diffuse_data.N = N;

  result = closure_eval(diffuse_data);
}
