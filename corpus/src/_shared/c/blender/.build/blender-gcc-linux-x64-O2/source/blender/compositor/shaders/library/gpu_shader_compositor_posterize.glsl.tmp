#line 1 "gpu_shader_compositor_posterize.glsl"
 
#line 6
void node_composite_posterize(float4 color, float steps, _ref(float4 ,result))
{
  float sanitized_steps = clamp(steps, 2.0f, 1024.0f);
  result = float4(floor(color.rgb * sanitized_steps) / sanitized_steps, color.a);
}
