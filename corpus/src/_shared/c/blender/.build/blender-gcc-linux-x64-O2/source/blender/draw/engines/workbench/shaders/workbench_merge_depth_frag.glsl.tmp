#line 1 "workbench_merge_depth_frag.glsl"
 
#line 7
FRAGMENT_SHADER_CREATE_INFO(workbench_merge_depth)

void main()
{
  float2 screen_uv = gl_FragCoord.xy / float2(textureSize(depth_tx, 0));
  gl_FragDepth = texture(depth_tx, screen_uv).r;
}
