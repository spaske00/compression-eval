#line 1 "workbench_effect_taa_frag.glsl"
 
#line 7
FRAGMENT_SHADER_CREATE_INFO(workbench_taa)

void main()
{
  float2 texel_size = 1.0f / float2(textureSize(color_buffer, 0));
  float2 uv = gl_FragCoord.xy * texel_size;

  frag_color = float4(0.0f);
  int i = 0;
#line 18
{
#line 16
                                           {
        {int y = -1;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(-1, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(-1, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(-1, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 25
    }
  }
#line 16
                                           {
        {int y = -1;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(0, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(0, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(0, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 25
    }
  }
#line 16
                                           {
        {int y = -1;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(1, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(1, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 17
                                                  {
      float4 color = texture(color_buffer, uv + float2(1, y) * texel_size);

      color = clamp(color, float4(0.0f), float4(1e10f));

      color.rgb = log2(color.rgb + 1.0f);

      frag_color += color * samplesWeights[i];
    }
#line 17
                             y++, i++;
#line 25
    }
  }
#line 26
  }
}
