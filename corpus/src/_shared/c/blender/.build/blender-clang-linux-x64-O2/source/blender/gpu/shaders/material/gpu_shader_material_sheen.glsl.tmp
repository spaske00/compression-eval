#line 1 "gpu_shader_material_sheen.glsl"
 
#line 9
void node_bsdf_sheen(float4 color, float roughness, float3 N, float weight, _ref(Closure ,result))
{
  color = max(color, float4(0.0f));
  roughness = saturate(roughness);
  N = safe_normalize(N);
#line 16
  ClosureDiffuse diffuse_data;
  diffuse_data.weight = weight;
  diffuse_data.color = color.rgb;
  diffuse_data.N = N;

  result = closure_eval(diffuse_data);
}
