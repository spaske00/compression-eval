#line 1 "subdiv_vbo_edituv_stretch_area_comp.glsl"
 
#line 7
COMPUTE_SHADER_CREATE_INFO(subdiv_edituv_stretch_area)

void main()
{

  uint quad_index = get_global_invocation_index();
  if (quad_index >= shader_data.total_dispatch_size) {
    return;
  }
#line 18
  uint start_loop_index = quad_index * 4;

  uint coarse_quad_index = coarse_face_index_from_subdiv_quad_index(
      quad_index, uint(shader_data.coarse_face_count));

  for (uint i = 0; i < 4; i++) {
    subdiv_stretch_area[start_loop_index + i] = coarse_stretch_area[coarse_quad_index];
  }
}
