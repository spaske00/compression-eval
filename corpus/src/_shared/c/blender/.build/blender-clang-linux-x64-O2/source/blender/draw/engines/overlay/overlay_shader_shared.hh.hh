static void metadata_overlay_shader_shared_hh_tmp(GPUSource &source, GPUFunctionDictionary *g_functions, GPUPrintFormatMap *g_formats) {
  source.add_dependency("GPU_shader_shared_utils.hh");
  source.add_printf_format(uint32_t(3022480497), "Ensure value is sync", g_formats);
  source.add_printf_format(uint32_t(3022480497), "Ensure value is sync", g_formats);
  source.add_printf_format(uint32_t(3022480497), "Ensure value is sync", g_formats);
  source.add_printf_format(uint32_t(3022480497), "Ensure value is sync", g_formats);
  UNUSED_VARS(source, g_functions, g_formats);
}
