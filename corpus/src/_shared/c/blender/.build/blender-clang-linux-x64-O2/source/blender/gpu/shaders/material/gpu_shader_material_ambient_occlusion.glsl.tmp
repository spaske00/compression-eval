#line 1 "gpu_shader_material_ambient_occlusion.glsl"
 
#line 8
void node_ambient_occlusion(float4 color,
                            float dist,
                            float3 normal,
                            const float inverted,
                            const float sample_count,
                            _ref(float4 ,result_color),
                            _ref(float ,result_ao))
{
  result_ao = ambient_occlusion_eval(safe_normalize(normal), dist, inverted, sample_count);
  result_color = result_ao * color;
}
